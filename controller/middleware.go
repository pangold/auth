package controller

import (
	// "fmt"
	"github.com/gin-gonic/gin"

	// "../utils"
)

func middleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		// TODO: anything you wanted, could be filter(by token)
	}
}

